import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const Book = () => (
  <View>
    <Text></Text>
  </View>
);

export default Book;

const styles = StyleSheet.create({
  book: {
    borderWidth: 1,
    borderColor: 'black',
    width: '30%',
    height: 100,
    margin: 5
    // flex: '1calc()'
  }
})
